/**
 * Source-side compatibility owner for the canonical machine-core layout runtime.
 * The browser runtime keeps its established public filename for compatibility.
 */
export {
  seatShellBranchId,
  bindSeatShellProjection,
  createBranchConnectionCore,
  getBranchCamera,
  resolveBranchCamera,
} from './machine-core-layout.js';
