import { createMachineTransition, resolveMachineCamera, projectMachinePoint } from './machine-hero-scene.js';
const S={id:'seat-0-connection',semanticId:'SEAT_CONNECTION',center:{x:-1.15,y:.55,z:0},dimensions:{x:1.7,y:.7,z:1.35},port:{x:-.28,y:.7,z:0}};
const T={id:'seat-0-behavior',semanticId:'SEAT_BEHAVIOR',center:{x:1.05,y:.55,z:.15},dimensions:{x:1.45,y:.7,z:1.2},port:{x:.32,y:.7,z:.15}};
function mount(){const host=document.querySelector('.hero-shell');if(!host||document.querySelector('[data-machine-hero-preview]'))return;const panel=document.createElement('aside');panel.className='machine-hero-preview';panel.dataset.machineHeroPreview='1';panel.innerHTML='<strong>Machine prototype</strong><span> semantic camera</span><button type="button" data-machine-nudge>Move geometry</button><output data-machine-state>subject follows geometry</output><canvas aria-label="Machine Hero semantic preview"></canvas>';host.append(panel);const c=panel.querySelector('canvas'),ctx=c.getContext('2d');let offset=0;const resize=()=>{const dpr=devicePixelRatio||1,r=c.getBoundingClientRect();c.width=Math.max(1,Math.round(r.width*dpr));c.height=Math.max(1,Math.round(r.height*dpr));ctx.setTransform(dpr,0,0,dpr,0,0);draw(r.width,r.height)};const transition=()=>createMachineTransition({seatIndex:0,source:S,target:{...T,center:{...T.center,x:T.center.x+offset,z:T.center.z+offset*.3},port:{...T.port,x:T.port.x+offset,z:T.port.z+offset*.3}},expansion:{sourceAmount:.88,targetAmount:.66},wiring:{id:'TREE-HERO-SEAT#0:SEAT_CONNECTION:ADJACENCY_WIRING'}});function draw(w,h){const t=transition(),cam=resolveMachineCamera({cameraId:'SEAT_CLOSE',subject:t.subject,viewport:{width:w,height:h}});ctx.clearRect(0,0,w,h);ctx.fillStyle='#f3efe7';ctx.fillRect(0,0,w,h);for(const p of [t.sourceGeometry,t.targetGeometry]){const q=projectMachinePoint(p.center,cam,{width:w,height:h});ctx.save();ctx.translate(q.x,q.y);ctx.beginPath();ctx.ellipse(0,0,Math.max(24,p.dimensions.x*cam.scale),Math.max(18,p.dimensions.z*cam.scale*.75),0,0,Math.PI*2);ctx.fillStyle=p.active?'#d8e9ff':'#eee7da';ctx.strokeStyle='#7d7568';ctx.fill();ctx.stroke();ctx.fillStyle='#37312a';ctx.font='600 12px system-ui,sans-serif';ctx.textAlign='center';ctx.fillText(p.semanticId.replace('SEAT_',''),0,4);ctx.restore()}const sc=projectMachinePoint(t.subject.center,cam,{width:w,height:h}),mn=projectMachinePoint(t.subject.min,cam,{width:w,height:h}),mx=projectMachinePoint(t.subject.max,cam,{width:w,height:h});ctx.save();ctx.setLineDash([5,4]);ctx.strokeStyle='#aa4e32';ctx.strokeRect(Math.min(mn.x,mx.x)-8,Math.min(mn.y,mx.y)-8,Math.abs(mx.x-mn.x)+16,Math.abs(mx.y-mn.y)+16);ctx.setLineDash([]);ctx.fillStyle='#aa4e32';ctx.beginPath();ctx.arc(sc.x,sc.y,4,0,Math.PI*2);ctx.fill();ctx.font='11px system-ui,sans-serif';ctx.fillText('semantic subject → camera target',sc.x+10,sc.y-8);ctx.restore()}panel.querySelector('[data-machine-nudge]').addEventListener('click',()=>{offset=offset?0:1.05;panel.querySelector('[data-machine-state]').textContent=offset?'geometry moved · target moved':'subject returned to base geometry';resize()});new ResizeObserver(resize).observe(c);addEventListener('resize',resize,{passive:true});resize()}
if(new URLSearchParams(location.search).has('machine-preview')){document.readyState==='loading'?document.addEventListener('DOMContentLoaded',mount,{once:true}):mount()}
