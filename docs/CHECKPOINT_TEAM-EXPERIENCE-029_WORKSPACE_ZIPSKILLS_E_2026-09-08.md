# Checkpoint — Slice E: WORKSPACE_ZIPSKILLS optional face

**Date:** 2026-09-08  
**Branch:** `feat/029-workspace-zipskills-optional-e`  
**Issue:** #154  
**Skills:** teamai-project → hierarchy-runtime → workspace-ring · seat-shell-hierarchy (boundary)

## Delivered

- Part ID `WORKSPACE_ZIPSKILLS` — workspace tree (R0), **not** in `SEAT_SHELL_V1_CHILDREN`
- Fixture `WORKSPACE_ZIPSKILLS_V1` — team-lead / shared / branch-before-main / external; all `optional: true`
- Inner-crown draw `drawWorkspaceZipskills` using §9 `RING_R0_ZIP_SCALE` (`0.22`)
- Keyboard `z` / `x` cycles R0; `.` still clears ring focus
- a11y: not required; external assignment allowed; not entitlement or authority (LAW 109)
- Docs: ring map, seat sheet, baseline §9, NEXT_SLICES (D merged, E merged (#155), F–J queued)

## Product rule

Not required platform config. Users may assign ZipSkills outside TeamAi. Possession is not merge / scheduler / entitlement authority.

## R1–R10 accounting

| Root | This slice |
|------|------------|
| R1 state | Deferred — no new parent open; uses existing ring-focus state |
| R2 pose | Named `RING_R0_ZIP_SCALE` inner crown |
| R3 camera | Deferred — no new dock |
| R4 input | `z`/`x` cycle R0; seat keys unchanged when shell open |
| R5 motion | Reduced-motion: no spin on ZipSkills torus |
| R6 display | Glass plates + torus; focused accent; stubs vs interactive held |
| R7 viewport | Same FOV contract; inner crown stays in workspace silhouette |
| R8 prefs | `data-motion` from documentElement only |
| R9 wiring | Presentation trajectories only |
| R10 theme | Isolation preserved — no second theme root |

## Next

After merge: next development phase F–J in `docs/TEAMAI_3D_HERO_NEXT_SLICES.md`. Recommended first: **G / #89** reduced-motion lighting (highest-leverage); **H** MECHANISM_ZIPSKILLS reconcile if e2e labels still legacy.

## Boundaries

Presentation only · no 029-released claim · Merge gate #133
