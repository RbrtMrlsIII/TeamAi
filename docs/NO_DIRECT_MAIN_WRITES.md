# Process Note

Subsequent stabilization work must use a feature branch and pull request rather than writing directly to `main`.

The current-state reconciliation documents already landed on `main`; no runtime/product authority was changed by those documentation-only additions.
