# Checkpoint — Cam-4 Edge-Drag + Inverse-Swipe (2026-09-09)

**Slice:** Cam-4
**Prior:** Cam-3 #193 merged
**Status:** FULFILLED MODULE PROVENANCE — browser depth verification remains a current evidence task, not a Cam-5 queue
**Claim:** presentation only · **no 029-released claim**

## Delivered

- `public/hero-cam4-edge-swipe.js` — edge zones, continuous drift, inverse-swipe deltas
- Pointer at screen edge → continuous orbit (skipped under reduced motion)
- Drag/swipe path → inverse orbit about the current tree center
- Apply path wires Cam-2 + Cam-3 + Cam-4

## Important distinction

The module implementation is fulfilled. What remains is evidence quality: Issue #232 calls for a real Hero browser proof tracing module → apply wiring → user input. Do not reopen Cam-4 architecture merely because that proof is still outstanding.

## Retired queue text

The previous “Optional next” list mixed follow-on experience work, DOM debt, and an architectural rewrite idea. Those are now classified separately:

- general-tree readability → spatial execution basis / V-series evidence
- interim DOM absorption → existing presentation debt
- static `hero-flex.js` rewrite → compatibility/apply-path integrity work, not an automatic architecture rewrite

## Current continuation

Use `docs/TEAMAI_029_CURRENT_STATE_MAP.md` and the Cam↔V reconciliation to select the current task.
