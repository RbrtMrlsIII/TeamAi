# Slice 031 — Seat-1 branch-wiring continuity

This slice consumes the existing `SEAT_CONNECTION` → `SEAT_BEHAVIOR` semantic wiring contract in the canonical Hero renderer.

The structural path is presentation-only. During source compaction, the target branch remains closed; after source compaction reaches zero, the same semantic path can reveal the target expansion. The path is derived from current semantic division ports and branch geometry, not fixed world-space coordinates.

This slice does not establish final electrical/turn-loop choreography, complete multi-tree topology, authorization, entitlement, scheduler authority, or durable-domain writes.
